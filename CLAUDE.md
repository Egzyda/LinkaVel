# LinkaVel — プロジェクト仕様 / 開発運用ルール

## 0. 最重要：Git 運用ルール（必ず守ること）

**コードを修正したら、毎回必ず `main` ブランチへ反映（プッシュまたはマージ）すること。**

- 作業ブランチで作業した場合も、動作確認が済んだ時点で **その都度 `main` にマージし、`origin/main` へプッシュする**。
- 「後でまとめてマージ」はしない。1 タスク＝1 コミット＝`main` 反映 を原則とする。
- コミットメッセージは日本語で、何を直したかが分かる形にする（例: `fix: 召喚コスト選択時のスロット誤判定を修正`）。
- 破壊的な変更（ルール改正・カードデータの大規模改訂）を行う場合のみ、事前に相談してから `main` に入れる。
- ゲームルール本体を変更した場合は `ルール.txt` を、効果ロジック仕様を変更した場合は `ロジック定義.txt` を **同じコミット内で** 更新する。

手順の例:

```bash
git add -A
git commit -m "fix: ○○を修正"
git checkout main && git merge --no-ff <作業ブランチ>
git push -u origin main
```

---

## 1. プロジェクト概要

ブラウザで動くカードゲーム「LinkaVel」。ビルド工程なし・静的ファイルのみ。
`index.html` を直接開けば動作する（Firestore を使うためローカルサーバ推奨）。

| ファイル | 役割 |
|---|---|
| `index.html` | 全画面（メニュー / デュエル / デッキ選択 / デッキビルダー）のDOM |
| `style.css` | デュエル画面・共通UI |
| `deck_builder.css` | デッキビルダー画面 |
| `cards.js` | カードマスタ `MASTER_CARDS` とスターターデッキ `DECK_RECIPES` |
| `main.js` | ゲーム状態 `GAME_STATE`、ターン進行、召喚・戦闘、描画、入力処理 |
| `effect_logic.js` | カード効果の汎用エンジン `EffectLogic` |
| `cpu_logic.js` | CPU の思考 `CpuLogic` |
| `deck_builder.js` | デッキ構築・Firestore 保存 |
| `tutorial.js` | 遊び方画面（タブ切り替え・カード実例の描画） |
| `firebase_config.js` | Firebase 初期化（匿名認証＋Firestore） |
| `img/game_icons.js` | イラスト未作成カードのフォールバック用アイコン（Game-icons.net、CC BY 3.0） |
| `ルール.txt` | ゲームルール仕様書（正典） |
| `ロジック定義.txt` | `logic` プロパティの共通規格（正典） |

## 2. 設計上の約束

- カードデータは `getCardData()` でディープコピーして使う。**同名カードは別インスタンス**として扱うこと。
  したがって手札やフィールドからカードを探すときは `findIndex(c => c.id === ...)` ではなく
  **オブジェクト同一性（`indexOf(cardData)`）** で探すこと。
- 効果は `MASTER_CARDS[].logic` の宣言データで表現し、`effect_logic.js` 側に実装を足す。
  カード個別の `if (card.id === "sXXX")` 分岐は極力増やさない。
- UI は 3D 変形したフィールド上のタップ判定を含むため、座標判定を変更したら
  **スマホ実機幅（360px 前後）と PC 幅の両方**で確認する。
- カードの `icon`（`img/game_icons.js` のキー）は「イラスト未作成」の印。
  `icon` があるカードは `<img>` を作らずアイコンを直接描画する（404 による点滅を避けるため）。
  **イラストを `img/<id>.webp` に用意したら、そのカードの `icon` 行を消すこと。**

## 3. 動作確認の目安

- スマホ縦（360×640 / 390×844）と PC（1280×800 以上）で、
  召喚 → コスト選択 → 攻撃 → エンドフェイズの一連が破綻なく操作できること。
- デッキビルダーでライブラリを最後までスクロールできること。
- 5 つのスターターデッキすべてで CPU 戦を完走できること。
