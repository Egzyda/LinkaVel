# LinkaVel プロジェクト ルール (Agent Rules)

## 1. Git プッシュの自動化ルール
* タスクによってコードやファイルの変更を行った場合は、必ずタスクの最後に `git add .`, `git commit -m "..."`, `git push` を実行し、変更をリモートリポジトリ（メインブランチ）へ反映させてください。
* 毎回プッシュを行うことで、作業の区切りごとに状態を確実に保存します。
